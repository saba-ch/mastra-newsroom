// Hand-off from the form to the run page: the form creates the run and stores its input under this prefix + runId,
// the run page starts it with that input.
export const PENDING_INPUT_PREFIX = "newsroom:start:";
